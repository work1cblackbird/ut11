
#Область СлужебныйПрограммныйИнтерфейс

Функция ХэшТипаДокументаИСтатуса(ТипДокументаСсылка, СтатусДокумента) Экспорт
	
	Данные = Новый Соответствие();
	Данные.Вставить("ТипДокумента", ТипДокументаСсылка);
	Данные.Вставить("Статус", СтатусДокумента);
	
	ХешированиеДанных = Новый ХешированиеДанных(ХешФункция.CRC32);
	ХешированиеДанных.Добавить(ЗначениеВСтрокуВнутр(Данные));
	
	Возврат ХешированиеДанных.ХешСумма;
	
КонецФункции

// Возвращает список команд EDI для указанной формы.
//
// Параметры:
//  Форма                   - ФормаКлиентскогоПриложения, Строка - форма или полное имя формы, для которой необходимо получить список команд EDI.
//  ДополнительныеПараметры - Структура                          - дополнительные параметры команд.
//
// Возвращаемое значение:
//  ТаблицаЗначений из См. ДокументыEDIИнтеграция.НоваяТаблицаКомандEDI
//
Функция КомандыEDIФормы(Знач Форма, Знач ДополнительныеПараметры) Экспорт
	
	Возврат ДокументыEDIИнтеграция.КомандыEDIФормы(Форма, ДополнительныеПараметры);
	
КонецФункции

#КонецОбласти