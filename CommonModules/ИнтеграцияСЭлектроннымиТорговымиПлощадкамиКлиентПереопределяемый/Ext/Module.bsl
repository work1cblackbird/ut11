// @strict-types
#Область ПрограммныйИнтерфейс

// Переопределяет формы подбора и параметры при их открытии.
// 
// Параметры:
//  ИмяКоманды - Строка - Команда, которая иницировала вызов процедуры
//  ФормаИмя - Строка - Например, "Обработка.ПодборТоваровПоОтбору.Форма"
//  ПараметрыФормы - Структура - Параметры открытия формы
Процедура ПриОпределенииФормыПодбораИПараметров(ИмяКоманды, ФормаИмя, ПараметрыФормы) Экспорт

	Если ИмяКоманды = "ДобавитьПоОтбору" Или ИмяКоманды = "УдалитьПоОтбору" Тогда
		ФормаИмя = "Обработка.ПодборТоваровПоОтбору.Форма";
	ИначеЕсли ИмяКоманды = "Подобрать" Тогда
		ФормаИмя = "Обработка.ПодборТоваровВДокументПродажи.Форма";
	КонецЕсли;

	Если ИмяКоманды = "ДобавитьПоОтбору" Или ИмяКоманды = "Подобрать" Тогда
		ПараметрЗаголовок = НСтр("ru = 'Подбор товаров для выгрузки в Бидзаар'");
		ПараметрыЗаголовокКнопкиПеренести = НСтр("ru = 'Добавить в выгрузку'");
	Иначе
		ПараметрЗаголовок = НСтр("ru = 'Исключение товаров из выгрузки в Бидзаар'");
		ПараметрыЗаголовокКнопкиПеренести = НСтр("ru = 'Исключить из выгрузки'");
	КонецЕсли;

	Если ИмяКоманды = "Подобрать" Тогда
		ПараметрыФормы.Вставить("Документ", Ложь);
		ПараметрыФормы.Вставить("ПоказыватьПодобранныеТовары", Истина);
		ПараметрыФормы.Вставить("ТребуетсяСохранение", Истина);
		ПараметрыФормы.Вставить("ИспользоватьФильтры", Истина);
		ПараметрыФормы.Вставить("РежимПодбораБезКоличественныхПараметров", Истина);
		ПараметрыФормы.Вставить("РежимПодбораБезСуммовыхПараметров", Истина);
		ПараметрыФормы.Вставить("РежимПодбораБезСоглашенийСКлиентами", Истина);
	КонецЕсли;

	ПараметрыФормы.Вставить("Заголовок", ПараметрЗаголовок);
	ПараметрыФормы.Вставить("ЗаголовокКнопкиПеренести", ПараметрыЗаголовокКнопкиПеренести);

КонецПроцедуры

#КонецОбласти
