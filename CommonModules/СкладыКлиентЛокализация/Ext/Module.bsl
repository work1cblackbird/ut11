
#Область СлужебныйПрограммныйИнтерфейс

// Параметры:
// 	Команда - КомандаФормы
// 	Форма - ФормаКлиентскогоПриложения:
// 		* Элементы - ВсеЭлементыФормы:
// 			** ОткрытьКатегорииЭксплуатации - КомандаФормы
// 
Процедура ПриВыполненииКоманды_ПанельСправочниковСклад(Команда, Форма) Экспорт

	//++ Локализация
	
	Элементы = Форма.Элементы;
	
	Если Команда.Имя = Элементы.ОткрытьКатегорииЭксплуатации.ИмяКоманды Тогда
		ОткрытьФорму("Справочник.КатегорииЭксплуатации.ФормаСписка", , Форма);
	КонецЕсли; 
	
	//-- Локализация
	
КонецПроцедуры

Процедура ДополнитьПоляСтрокиТабличнойЧастиПриИзмененииКачества(СтрокаОприходования, СтруктураДействий) Экспорт

	//++ Локализация
	
	Если СтруктураДействий.Свойство("ЗаполнитьПризнакМаркируемаяПродукция") Тогда
		СтрокаОприходования.Вставить("МаркируемаяПродукция");
	КонецЕсли;
	
	Если СтруктураДействий.Свойство("ЗаполнитьВидПродукцииИС") Тогда
		СтрокаОприходования.Вставить("ВидПродукцииИС");
	КонецЕсли;
	
	//-- Локализация
	
КонецПроцедуры

#КонецОбласти

