
#Область СлужебныеПроцедурыИФункции

Процедура КонтрольОтображенияПротоколаВзаимодействия() Экспорт
	
	ФормыПротоколаВзаимодействия = ДокументыEDIИнтеграцияКлиент.ФормыПротоколаВзаимодействия();
	
	Если ФормыПротоколаВзаимодействия.Обработка Тогда
		Возврат;
	КонецЕсли;
	
	ФормыПротоколаВзаимодействия.Обработка = Истина;
	Попытка
		ДокументыEDIИнтеграцияКлиент.ВыполнитьКонтрольОтображенияПротоколаВзаимодействия(ФормыПротоколаВзаимодействия.Список);
		
		ФормыПротоколаВзаимодействия.Обработка = Ложь;
	Исключение
		ФормыПротоколаВзаимодействия.Обработка = Ложь;
		ВызватьИсключение;
	КонецПопытки;
	
КонецПроцедуры

Процедура КонтрольВыполненияСинхронизации() Экспорт
	
	ВыполняемыеСинхронизацииДокументов = ДокументыEDIИнтеграцияКлиент.ВыполняемыеСинхронизацииДокументов();
	
	Если ВыполняемыеСинхронизацииДокументов.Обработка Тогда
		Возврат;
	КонецЕсли;
	
	ВыполняемыеСинхронизацииДокументов.Обработка = Истина;
	Попытка
		ДокументыEDIИнтеграцияКлиент.ВыполнитьКонтрольСинхронизации(ВыполняемыеСинхронизацииДокументов.Список);
		
		ВыполняемыеСинхронизацииДокументов.Обработка = Ложь;
	Исключение
		ВыполняемыеСинхронизацииДокументов.Обработка = Ложь;
		ВызватьИсключение;
	КонецПопытки;
	
КонецПроцедуры

#КонецОбласти 