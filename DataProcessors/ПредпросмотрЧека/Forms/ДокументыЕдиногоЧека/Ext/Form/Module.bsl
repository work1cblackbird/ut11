
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("АвтоТест") Тогда // Возврат при получении формы для анализа.
		Возврат;
	КонецЕсли;
	
	МассивДокументовЕдиногоЧека = Параметры.ДокументыЕдиногоЧека;
	Для Каждого ДанныеДокумента Из МассивДокументовЕдиногоЧека Цикл
		СтрокаДокументыЕдиногоЧека = ДокументыЕдиногоЧека.Добавить();
		СтрокаДокументыЕдиногоЧека.Документ = ДанныеДокумента.Документ;
		СтрокаДокументыЕдиногоЧека.Сумма = ДанныеДокумента.Сумма;
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ДокументыЕдиногоЧекаВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	СтандартнаяОбработка = Ложь;

	ЗначениеДляПоказа = Неопределено;
	Если ТипЗнч(ВыбраннаяСтрока) = Тип("Массив") Тогда
		Если ВыбраннаяСтрока.Количество() = 1 Тогда
			ЗначениеДляПоказа = ДокументыЕдиногоЧека[ВыбраннаяСтрока[0]].Документ;
		Иначе
			Возврат;
		КонецЕсли;
	Иначе
		ЗначениеДляПоказа = ДокументыЕдиногоЧека[ВыбраннаяСтрока].Документ;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ЗначениеДляПоказа) Тогда
		ПоказатьЗначение(Неопределено, ЗначениеДляПоказа);
	КонецЕсли;
КонецПроцедуры

#КонецОбласти