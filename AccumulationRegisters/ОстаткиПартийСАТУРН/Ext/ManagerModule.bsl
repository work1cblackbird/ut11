#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область СлужебныйПрограммныйИнтерфейс

// Записывает движения документа в регистр.
// 
// Параметры:
//  ДополнительныеСвойства - Структура - Дополнительные свойства:
//   * ТаблицыДляДвижений - Структура - таблицы движений документа:
//     ** ТаблицаОстаткиПартийСАТУРН - см. ПустаяТаблицаОстаткиПартийСАТУРН
//  Движения - Структура - движения документа по регистрам:
//    * ОстаткиПартийСАТУРН - РегистрНакопленияНаборЗаписей.ОстаткиПартийСАТУРН - движения документа
//  Отказ - Булево - Отказ от проведения / записи движений
Процедура ОтразитьДвижения(ДополнительныеСвойства, Движения, Отказ) Экспорт
	
	Если Отказ Или ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаОстаткиПартийСАТУРН.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	
	ДвиженияОстаткиПартийСАТУРН = Движения.ОстаткиПартийСАТУРН;
	ДвиженияОстаткиПартийСАТУРН.Записывать = Истина;
	ДвиженияОстаткиПартийСАТУРН.Загрузить(ДополнительныеСвойства.ТаблицыДляДвижений.ТаблицаОстаткиПартийСАТУРН);
	
КонецПроцедуры

// Обновить движения при изменении статуса документа.
// 
// Параметры:
//  ДокументСсылка - ДокументСсылка - регистратор текущего регистра
Процедура ОбновитьДвиженияПриИзмененииСтатусаДокумента(ДокументСсылка) Экспорт
	
	НаборЗаписей = СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.Регистратор.Установить(ДокументСсылка);
	
	ДополнительныеСвойства = Новый Структура;
	ИнтеграцияИС.ИнициализироватьДополнительныеСвойстваДляПроведения(ДокументСсылка, ДополнительныеСвойства);
	Документы[ДополнительныеСвойства.ДляПроведения.МетаданныеДокумента.Имя].ИнициализироватьДанныеДокумента(
		ДокументСсылка, ДополнительныеСвойства, "ОстаткиПартийСАТУРН");
	НаборЗаписей.Загрузить(ДополнительныеСвойства.ТаблицыДляДвижений["ТаблицаОстаткиПартийСАТУРН"]);
	НаборЗаписей.Записать();
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Пустая таблица остатки партий САТУРН.
// 
// Возвращаемое значение:
//  ТаблицаЗначений - Пустая таблица остатки партий САТУРН
Функция ПустаяТаблицаОстаткиПартийСАТУРН() Экспорт
	Возврат СоздатьНаборЗаписей().Выгрузить();
КонецФункции

#КонецОбласти

#КонецЕсли
