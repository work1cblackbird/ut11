#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("АвтоТест") Тогда // Возврат при получении формы для анализа.
		Возврат;
	КонецЕсли;
	
	Период = НачалоМесяца(Параметры.Период);
	Страна = Параметры.Страна;
	ПоВыделеннымСтрокам = Параметры.ПоВыделеннымСтрокам;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура Начать(Команда)
	
	Если ПроверитьЗаполнение() Тогда
		
		Результат = Новый Структура;
		Результат.Вставить("Период", Период);
		Результат.Вставить("Страна", Страна);
		Результат.Вставить("СтавкаНДС", СтавкаНДС);
		Результат.Вставить("ПоВыделеннымСтрокам", ПоВыделеннымСтрокам);
		
		Закрыть(Результат);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПериодПриИзменении(Элемент)
	Период = НачалоМесяца(Период);
КонецПроцедуры

#КонецОбласти