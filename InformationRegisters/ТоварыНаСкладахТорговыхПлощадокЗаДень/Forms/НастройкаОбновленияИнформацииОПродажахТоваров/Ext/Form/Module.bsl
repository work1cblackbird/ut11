
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ТекущаяДата                        = ТекущаяДатаСеанса();
	ПериодЗагрузкиДанных.ДатаНачала    = НачалоДня(ДобавитьМесяц(ТекущаяДата, -3));
	ПериодЗагрузкиДанных.ДатаОкончания = КонецДня(ТекущаяДата);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ОК(Команда)
	
	Результат = Новый Структура;    
	Результат.Вставить("УчетнаяЗаписьТорговойПлощадки", УчетнаяЗаписьТорговойПлощадки);
	Результат.Вставить("НачалоПериода",                 ПериодЗагрузкиДанных.ДатаНачала);
	Результат.Вставить("ОкончаниеПериода",              ПериодЗагрузкиДанных.ДатаОкончания);
	
	Закрыть(Результат);
	
КонецПроцедуры

#КонецОбласти

