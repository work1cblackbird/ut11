// @strict-types

#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область СлужебныеПроцедурыИФункции

// Параметры:
//  Идентификатор - Строка
// 
// Возвращаемое значение:
//  Булево
Функция ЕстьПравило(Идентификатор) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ
		|	СостоянияПравилВизуализацииОблачногоЭДО.ИдентификаторПравила
		|ИЗ
		|	РегистрСведений.СостоянияПравилВизуализацииОблачногоЭДО КАК СостоянияПравилВизуализацииОблачногоЭДО
		|ГДЕ
		|	СостоянияПравилВизуализацииОблачногоЭДО.ИдентификаторПравила = &ИдентификаторПравила";
	
	Запрос.УстановитьПараметр("ИдентификаторПравила", Идентификатор);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	Возврат Не РезультатЗапроса.Пустой();
	
КонецФункции

// Параметры:
//  ИдентификаторПравила - Строка
//  ДатаОбновления - Дата
Процедура Записать(ИдентификаторПравила, ДатаОбновления) Экспорт
	НаборЗаписей = СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.ИдентификаторПравила.Установить(ИдентификаторПравила);
	ЗаписьНабора = НаборЗаписей.Добавить();
	ЗаписьНабора.ИдентификаторПравила = ИдентификаторПравила;
	ЗаписьНабора.ДатаОбновления = ДатаОбновления;
	НаборЗаписей.Записать();
КонецПроцедуры

#КонецОбласти

#КонецЕсли
