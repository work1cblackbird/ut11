#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда
	
Процедура ПередЗаписью(Отказ)
	
	Если ОбменДанными.Загрузка = Истина Тогда
		Возврат;
	КонецЕсли;
	
	#Если Сервер Тогда
	МассивРеквизитов = Новый Массив;
	Для Каждого СтрокаТЧ Из Данные Цикл
		Если СтрокаТЧ.ИмяТабличнойЧасти = "" Тогда
			СтруктураРеквизита = Новый Структура;
			СтруктураРеквизита.Вставить("Имя", СтрокаТЧ.Имя);
			СтруктураРеквизита.Вставить("Значение", СтрокаТЧ.Значение);
			СтруктураРеквизита.Вставить("Представление", СтрокаТЧ.Представление);
			МассивРеквизитов.Добавить(СтруктураРеквизита);
		КонецЕсли;			
	КонецЦикла;
	
	Если ЗначениеЗаполнено(ИмяОбъекта) Тогда
		Наименование = ОбменСГИСЭПД.ПредставлениеХранимыхДанныхЭПД(МассивРеквизитов, ИмяОбъекта, ГруппаДанных);
	КонецЕсли;
	#КонецЕсли
		
КонецПроцедуры

#КонецЕсли
