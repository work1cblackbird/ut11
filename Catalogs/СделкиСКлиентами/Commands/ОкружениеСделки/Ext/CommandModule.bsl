
#Область ОбработчикиСобытий

&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	
	СделкаИсточник = ПараметрыВыполненияКоманды.Источник.Объект;
	УникальныйИдентификатор = ПараметрыВыполненияКоманды.Источник.УникальныйИдентификатор;
	
	ОткрытьФорму(
		"Справочник.СделкиСКлиентами.Форма.ОкружениеСделки",
		Новый Структура("АдресУчастники,Партнер",
		       АдресКомандыВоВременномХранилище(СделкаИсточник, УникальныйИдентификатор),
		       СделкаИсточник.Партнер),
		    ПараметрыВыполненияКоманды.Источник,
		    ПараметрыВыполненияКоманды.Уникальность,
		    ПараметрыВыполненияКоманды.Окно);

КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Функция АдресКомандыВоВременномХранилище(СделкаИсточник, УникальныйИдентификатор)

	Возврат ПоместитьВоВременноеХранилище(СделкаИсточник.ПартнерыИКонтактныеЛица.Выгрузить(),УникальныйИдентификатор)

КонецФункции

#КонецОбласти
