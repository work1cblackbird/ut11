
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("АвтоТест") Тогда // Возврат при получении формы для анализа.
		Возврат;
	КонецЕсли;

КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура КомандаОтмена(Команда)
	
	Результат = ДанныеРезультатаЗапросаАдресаМагазина();
	Закрыть(Результат);
	
КонецПроцедуры

&НаКлиенте
Процедура КомандаОк(Команда)
	
	Результат = Новый Структура("Результат,АдресРасчетов,МестоРасчетов", КодВозвратаДиалога.ОК, СокрЛП(АдресРасчетов), СокрЛП(МестоРасчетов));
	Закрыть(Результат);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаКлиенте
Функция ДанныеРезультатаЗапросаАдресаМагазина()
	Возврат Новый Структура("Результат,АдресМагазина,МестоРасчетов", КодВозвратаДиалога.Отмена, Неопределено, Неопределено);
КонецФункции
	
#КонецОбласти
