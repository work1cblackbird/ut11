&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	                                                           
	ОбменСГИСЭПД.ПриСозданииНаСервереПодчиненнойФормы(ЭтотОбъект, Отказ, СтандартнаяОбработка);
	
КонецПроцедуры

&НаКлиенте
Процедура ТитулВыпускОтветственныйЗаСостояниеТСФамилияНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	
	ПараметрыВыбора = Новый Структура;
	ПараметрыВыбора.Вставить("РежимВыбора",Истина);

	ОбработкаВыбора = Новый ОписаниеОповещения("ВыбратьОтветственного_Завершение", ЭтотОбъект, );

	ОткрытьФорму("Справочник.ФизическиеЛица.ФормаВыбора", ПараметрыВыбора,
	        ЭтотОбъект, , , , ОбработкаВыбора);
	        
КонецПроцедуры

&НаКлиенте
Процедура ВыбратьОтветственного_Завершение(Значение, ТекущаяСтрока) Экспорт
	
	Если Значение <> Неопределено Тогда  
		РеквизитыКонтрагента = РеквизитыФизЛица(Значение);
		РеквизитыКонтрагента.Свойство("Фамилия", ТитулВыпускОтветственныйЗаСостояниеТСФамилия);
		РеквизитыКонтрагента.Свойство("Имя", ТитулВыпускОтветственныйЗаСостояниеТСИмя);
		РеквизитыКонтрагента.Свойство("Отчество", ТитулВыпускОтветственныйЗаСостояниеТСОтчество);
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция РеквизитыФизЛица(ФизЛицо)
	
	Возврат ОбменСГИСЭПД.ПолучитьРеквизитыФизЛица(ФизЛицо);
	
КонецФункции


&НаКлиенте
Процедура Сохранить(Команда)
		
	ОбменСГИСЭПДКлиент.СохранитьПараметрыПодчиненнойФормы(ЭтотОбъект);
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	ОбменСГИСЭПДКлиент.ПриОткрытииПодчиненнойФормы(ЭтотОбъект);
																		
КонецПроцедуры
			
&НаКлиенте
Функция ОписаниеРеквизитовФормы() Экспорт
	
	Возврат ОписаниеРеквизитовФормыСервер();
	
КонецФункции

&НаСервере
Функция ОписаниеРеквизитовФормыСервер()
	
	Возврат ОбменСГИСЭПД.ОписаниеРеквизитовФормы(ЭтаФорма);
		
КонецФункции