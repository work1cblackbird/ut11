#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Массив статусов, в которых проведенный документ требует резервирования исходной партии.
// 
// Возвращаемое значение:
//  Массив из ПеречислениеСсылка.СтатусыОбработкиОформлениеСДИЗЗЕРНО - Требует резервирования партии
Функция ТребуетРезервированияПартии() Экспорт
	
	Результат = Новый Массив;
	Результат.Добавить(Черновик);
	Результат.Добавить(СписаниеПартииКПередаче);
	Результат.Добавить(СписаниеПартииОбрабатывается);
	Результат.Добавить(СписаниеПартииОшибкаПередачи);
	Результат.Добавить(ПартииСписаныЧастично);
	Результат.Добавить(Ошибка);
	Возврат Результат;
	
КонецФункции

// Массив статусов, в которых проведенный документ требует списания исходной партии.
// 
// Возвращаемое значение:
//  Массив Из ПеречислениеСсылка.СтатусыОбработкиОформлениеСДИЗЗЕРНО - Требует списания партии
Функция ТребуетСписанияПартии() Экспорт
	
	Результат = Новый Массив;
	Результат.Добавить(ПартииСписаны);
	Результат.Добавить(АннулированиеКПередаче);
	Результат.Добавить(АннулированиеОбрабатывается);
	Результат.Добавить(АннулированиеОшибкаПередачи);
	Возврат Результат;
	
КонецФункции

#КонецОбласти

#КонецЕсли