//@strict-types

#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда
	
#Область ОбработчикиСобытий

Процедура ОбработкаПолученияДанныхВыбора(ДанныеВыбора, Параметры, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	ИспользуютсяСегменты = ПолучитьФункциональнуюОпцию("ИспользоватьСегментыПартнеров");
	ИспользуютсяДоговораСКлиентами = ПолучитьФункциональнуюОпцию("ИспользоватьДоговорыСКлиентами");
	ИспользуютсяТиповыеСоглашенияСКлиентами = ПолучитьФункциональнуюОпцию("ИспользоватьТиповыеСоглашенияСКлиентами");
	ИспользуютсяИндивидуальныеСоглашенияСКлиентами = ПолучитьФункциональнуюОпцию("ИспользоватьИндивидуальныеСоглашенияСКлиентами");
	
	ДанныеВыбора = Новый СписокЗначений; // СписокЗначений из ПеречислениеСсылка.СоставыУчастниковРетроБонусов
	ДанныеВыбора.Добавить(ИНН);
	ДанныеВыбора.Добавить(Контрагенты);
	ДанныеВыбора.Добавить(Клиенты);
	
	Если ИспользуютсяСегменты Тогда
		ДанныеВыбора.Добавить(СегментыПартнеров);
	КонецЕсли;
	
	Если ИспользуютсяДоговораСКлиентами Тогда
		ДанныеВыбора.Добавить(Договоры);
	КонецЕсли;
	
	Если ИспользуютсяТиповыеСоглашенияСКлиентами
	 ИЛИ ИспользуютсяИндивидуальныеСоглашенияСКлиентами Тогда
		ДанныеВыбора.Добавить(Соглашения);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли
